// import functions and grab DOM elements
const submitButton = document.getElementById('click-submit');
const resetButton = document.getElementById('reset-button');
const userNumber = document.getElementById('user-guess');
const attemptsLeft = document.getElementById('attempts');
const notifyUser = document.getElementById('notify');

// initialize state


// set event listeners to update state and DOM